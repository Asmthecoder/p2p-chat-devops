apiVersion: v1
kind: Service
metadata:
  name: p2p-chat-service
  namespace: p2p-chat
spec:
  type: LoadBalancer
  selector:
    app: p2p-chat
  ports:
    - name: web
      port: 80
      targetPort: 17001
    - name: p2p
      port: 9001
      targetPort: 9001
